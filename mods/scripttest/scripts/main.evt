-- Increments a global work slot every frame.
--
-- gw[30] is used because the game does not touch it; gw[10], tried first, is
-- written by the game's own scripts and made the reading meaningless.
script main {
    loop {
        gw[30] = gw[30] + 1
        wait(1)
    }
}
