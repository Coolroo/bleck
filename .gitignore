# Game data — never commit. Large, and not ours to distribute.
roms/
extracted/

# Mod overlays contain extracted game assets — manifests are committed,
# contents are not. Remove this line for a mod of wholly original work.
mods/*/overlay/
build/

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
build/
dist/
